# See `.env.development` and `.env.production` for default API targets per environment.
# Override here only if you need a custom setup.
# VITE_API_URL=https://your-backend.example.com/api

VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=

# Optional: override the N8N webhook endpoint.
# - Dev default: /n8n/webhook/nit-multi-analyzer (uses Vite proxy if configured)
# - Prod default: https://kabir2512.app.n8n.cloud/webhook/nit-multi-analyzer
# VITE_N8N_WEBHOOK_URL=https://kabir2512.app.n8n.cloud/webhook/nit-multi-analyzer

# Optional: dev-only proxy target if you need to map /n8n to a different n8n host
# VITE_N8N_PROXY=https://kabir2512.app.n8n.cloud
